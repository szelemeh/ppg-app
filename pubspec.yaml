name: ppg_hrv_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^7.0.0
  camera: ^0.8.1
  empty_widget: ^0.0.3
  ffi: ^1.0.0
  fl_chart: ^0.35.0
  flutter_bloc: ^7.0.0
  http: ^0.13.3
  image: ^3.0.2
  intl: ^0.17.0
  path_provider: ^2.0.1
  percent_indicator: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
